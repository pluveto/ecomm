#pragma once

#include <stdio.h>
#include <stdint.h>
#include <string>

namespace ecomm
{
    namespace model
    {

        struct user_model_abstract
        {
            
        public:

            uint64_t id;
            double balance;
            char username[32];
            char password[48];
            uint64_t created_at;
            uint64_t updated_at;

            char role[8];

            virtual std::string role();

        };
        
        
    }
}
