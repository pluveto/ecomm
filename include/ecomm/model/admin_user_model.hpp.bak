#pragma once

#include <stdio.h>
#include <stdint.h>
#include <string>
#include <ecomm/model/user_model_abstract.hpp>
namespace ecomm
{
    namespace model
    {

        class admin_user_model : public user_model_abstract
        {
        private:
            bool role(std::string role);

        public:
            // getters
            uint64_t id();
            std::string role() override;
            double balance();
            std::string username();
            std::string password();
            uint64_t created_at();
            uint64_t updated_at();

            //setters
            void id(uint64_t id);
            void balance(double balance);
            bool username(std::string username);
            bool password(std::string password);
            void created();
            void updated();
        };
    }
}
